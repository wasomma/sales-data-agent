# Secrets
.env

# Data — never commit company sales data
data/
*.duckdb
*.duckdb.wal
*.xlsx
*.xlsm
*.pptx
*.csv

# Exception: sample-data/ holds SYNTHETIC demo files only (see its README).
# Never put real exports there — they would be committed.
!sample-data/*.xlsx
!sample-data/*.pptx

# demo/recording.json IS committed — it is what the public demo replays.
# It contains answers, i.e. actual figures from whatever data was loaded when
# `sales record-demo` ran. Only ever record against the synthetic dataset.

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Virtualenv
.venv/
